You are an expert C++23 build-fix and performance engineer.

Task: fix this optimized count_matches.cpp so the whole SUT shared library compiles successfully.
The immediate goal is to restore a valid build.

Hard requirements:
- Return one complete replacement for count_matches.cpp.
- Preserve the public C ABI exactly as declared in library.h.
- Do not change any exported function name, parameter type, return type, struct layout, ownership rule, or allocation/freeing convention visible in library.h.
- Do not add implementations for unrelated exported functions from library.h.
- Preserve the intended optimized behavior and runtime-performance focus as much as possible.
- Do not modify library.h or sut_common.h.
- Return raw C++ only.
- No Markdown, explanation, notes, or code fences.

Headers:
// library.h
#ifndef SUT_LIBRARY_H
#define SUT_LIBRARY_H

#include <stddef.h>
#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

typedef struct WordCount {
    char* word;
    uint64_t count;
} WordCount;

uint64_t fibonacci(uint64_t n);
char* format_list(const int* input, size_t input_length);
int64_t repeated_sort(const int* input, size_t input_length, int rounds);
size_t count_matches(
    const int* allowed,
    size_t allowed_length,
    const int* queries,
    size_t queries_length
);
WordCount* top_words_from_file(
    const char* path,
    const char* const* ignore_words,
    size_t ignore_words_length,
    size_t max_results,
    size_t* result_length
);
void free_string(char* value);
void free_word_counts(WordCount* values, size_t length);

#ifdef __cplusplus
}
#endif

#endif // SUT_LIBRARY_H


// sut_common.h
#ifndef SUT_COMMON_H
#define SUT_COMMON_H

#include <string>

char* copy_to_c_string(const std::string& value);
bool is_word_char(char c);
char normalize_char(char c);

#endif // SUT_COMMON_H


Failed count_matches.cpp:
#include "library.h"

#include <span>

static bool contains(const std::span<const int> input, const int needle) {
    return std::find(input.begin(), input.end(), needle) != input.end();
}

extern "C" {

size_t count_matches(
    const int* allowed,
    size_t allowed_length,
    const int* queries,
    size_t queries_length
) {
    if ((allowed == nullptr && allowed_length != 0) ||
        (queries == nullptr && queries_length != 0)) {
        return 0;
    }

    const std::span<const int> allowed_span{allowed, allowed_length};
    std::size_t matches = 0;

    for (std::size_t i = 0; i < queries_length; ++i) {
        if (contains(allowed_span, queries[i])) {
            ++matches;
        }
    }

    return matches;
}

}


Diagnostics:
Compiler/linker diagnostics:

STDERR:
In file included from /home/tijl/code/llmO/benchmark-builds/llm-cpp-remarks/qwen2_5_coder_14b_q4km/count_matches_cpp/iteration_02/attempt_01/source_output.cpp:3:
In file included from /usr/lib/gcc/x86_64-redhat-linux/14/../../../../include/c++/14/span:43:
In file included from /usr/lib/gcc/x86_64-redhat-linux/14/../../../../include/c++/14/array:43:
/usr/lib/gcc/x86_64-redhat-linux/14/../../../../include/c++/14/bits/stl_algobase.h:952:11: error: read-only variable is not assignable
  952 |         *__first = __tmp;
      |         ~~~~~~~~ ^
/usr/lib/gcc/x86_64-redhat-linux/14/../../../../include/c++/14/bits/stl_algobase.h:981:12: note: in instantiation of function template specialization 'std::__fill_a1<const int *, int>' requested here
  981 |     { std::__fill_a1(__first.base(), __last.base(), __value); }
      |            ^
/usr/lib/gcc/x86_64-redhat-linux/14/../../../../include/c++/14/bits/stl_algobase.h:998:12: note: in instantiation of function template specialization 'std::__fill_a1<const int *, std::span<const int>, int>' requested here
  998 |     { std::__fill_a1(__first, __last, __value); }
      |            ^
/usr/lib/gcc/x86_64-redhat-linux/14/../../../../include/c++/14/bits/stl_algobase.h:1029:12: note: in instantiation of function template specialization 'std::__fill_a<__gnu_cxx::__normal_iterator<const int *, std::span<const int>>, int>' requested here
 1029 |       std::__fill_a(__first, __last, __value);
      |            ^
/home/tijl/code/llmO/benchmark-builds/llm-cpp-remarks/qwen2_5_coder_14b_q4km/count_matches_cpp/iteration_02/attempt_01/source_output.cpp:6:17: note: in instantiation of function template specialization 'std::fill<__gnu_cxx::__normal_iterator<const int *, std::span<const int>>, int>' requested here
    6 |     return std::find(input.begin(), input.end(), needle) != input.end();
      |                 ^
/home/tijl/code/llmO/benchmark-builds/llm-cpp-remarks/qwen2_5_coder_14b_q4km/count_matches_cpp/iteration_02/attempt_01/source_output.cpp:6:17: error: no member named 'find' in namespace 'std'
    6 |     return std::find(input.begin(), input.end(), needle) != input.end();
      |            ~~~~~^
2 errors generated.

